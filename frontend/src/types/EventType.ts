export type EventType = {
  id?: number;
  title: string;
  description?: string;
  type?: string
  location: string;
};

export type FormData = {
  title: string,
  description: string,
  type: string,
  location: string
}